std := import("std")

funcs := []
for i := 0; i < 3; i++ {
    funcs = std.core.append(funcs, func() int { return i })
}

std.io.println(funcs[0]())
std.io.println(funcs[1]())
std.io.println(funcs[2]())
