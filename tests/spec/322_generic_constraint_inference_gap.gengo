// Constraint checks currently apply to explicit type arguments only.
// This inferred bool call succeeds even though bool is not numeric.
std := import("std")

func identity_numeric[T: numeric](x T) T { return x }

std.io.println(identity_numeric(true))
