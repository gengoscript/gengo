std := import("std")

type User struct { id int, name string, active bool }
u := User{ id: 7, name: "mikael", active: true }
std.io.println(u.name)
u.name = "mk"
std.io.println(u.name, std.core.len(u))
