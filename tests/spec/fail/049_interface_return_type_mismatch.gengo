type Parser interface {
    parse(src string) (int, error)
}

type Csv struct {}

func (c Csv) parse(src string) (int, error) {
    return 1, "bad"
}

func use(p Parser) {
    p.parse("x")
}

use(Csv{})
