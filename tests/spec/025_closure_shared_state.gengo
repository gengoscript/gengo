std := import("std")

mk := func() {
  x := 0
  inc := func(n int) { x += n; return x }
  dec := func(n int) { x -= n; return x }
  return [inc, dec]
}

pair := mk()
inc := pair[0]
dec := pair[1]
std.io.println(inc(5))
std.io.println(dec(2))
std.io.println(inc(1))
