std := import("std")

type Score int predicate func(x) { return x >= 0 and x <= 100 }

total := 0
for i := 0; i < 1000; i++ {
    s := Score(i rem 101)
    total = total + int(s)
}
std.io.println(total)
