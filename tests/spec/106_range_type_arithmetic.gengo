std := import("std")

type MyInt int range 1..20

// Compute in plain int, then box — int() truncates (same as Ada integer division)
a := 12
b := 15
m := MyInt(int((a + b) / 2))

count := 0
i := 1
for i <= m {
    count++
    i++
}
std.io.println(m)
std.io.println(count)
