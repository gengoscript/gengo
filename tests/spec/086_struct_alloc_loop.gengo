std := import("std")

type Point struct {
    x int,
    y int,
}

total := 0
i := 0
for i < 5000 {
    p := Point{ x: i, y: i * 2 }
    total = total + p.x + p.y
    i = i + 1
}

std.io.println(total)
