std := import("std")

type Target struct { value: int }

func test_named(t Target) {
  std.io.println(t.value)
}

test_anon := func(t Target) {
  std.io.println(t.value)
}

obj := Target{ value: 100 }
test_named(obj)
test_anon(obj)
